---
apiVersion: v1
kind: Service
metadata:
  name: mailbridge
spec:
  selector:
    app.kubernetes.io/name: mailbridge
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
